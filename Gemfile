source 'https://rubygems.org'

# Not sure why these need to be specified here, gemspec alone seems not to be enough
group :test do
  gem 'rails', '>= 3.2'
  gem 'doorkeeper', '>= 2.0'
  gem 'memory_test_fix'
  gem 'warden'
end

gemspec
